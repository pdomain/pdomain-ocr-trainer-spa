"""Training glue — torch-free mapping between SPA runs and pdomain-ocr-training (spec 02-backend §4.3).

This package contains no torch / DocTR code. The concrete LocalTrainingRunner
runs only inside the worker subprocess (D-T1).
"""
